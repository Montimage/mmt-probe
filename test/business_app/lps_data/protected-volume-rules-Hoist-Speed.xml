<beginning>

<embedded_functions><![CDATA[
//any numeric values in mmt-security are "double" 
// (even integer ou float, they are stored in a double)

static bool em_coor(double pos, double spd_ref){
       bool result = true;
       if(((pos < 45) && (pos > 43)) || ((pos > 47) && (pos < 49))){
              if (spd_ref!=0){
                     result = false;
              }
                     
       }
       return result;
}

double em_threshold(){
	static double val = -1;
}
]]></embedded_functions>

<property property_id="114" type_property="SECURITY" value="THEN"
    description="Hoist approaching to protected volume with speed != 0">
    <event event_id="1" value="COMPUTE" description="3 first markers"
           boolean_expression="( true )"/>
    <event event_id="2" value="COMPUTE" description="Negative acceleration not detected for Hoist"
           boolean_expression="(#em_coor(lps_data.MhPos, lps_data.MhSpdRef))"/>
 </property>

</beginning>
